# Minimal dependencies for the GROCERY collectors only (Flipp + Whole Foods).
# Used by the cloud cron (GitHub Actions) and scripts/refresh_grocery.py.
# No Scrapy/Playwright/lxml/BeautifulSoup — those are only for the (disabled)
# research scraper, so the cloud install stays tiny and fast.
requests>=2.32
supabase>=2.9
pydantic>=2.9
python-dotenv>=1.0.1
rich>=13.9
